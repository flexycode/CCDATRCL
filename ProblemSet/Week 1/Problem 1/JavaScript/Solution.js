// Loop through numbers from 1 to 100
for (let i = 1; i <= 100; i++) {
    // Check if the current number is even (i.e., divisible by 2)
    if (i % 2 === 0) {
        // If it's even, log the number to the console
        console.log(i);
    }
}