// Loop through numbers from 1 to 100
for (let i = 1; i <= 100; i++) {
    // Check if the current number is divisible by both 3 and 5
    if (i % 3 === 0 && i % 5 === 0) {
        // If it's divisible by both, log the number to the console
        console.log(i);
    }
}